node_modules
# Keep environment variables out of version control
.env

# Mobile / Expo
mobile/.expo/
mobile/dist/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

*.mp4
*.wav
*.mp3

# Blender auto-backups
*.blend1

# Shared build output
shared/dist/

# TypeScript build cache
*.tsbuildinfo

streaming/
videos/

# Backend build output (regenerated by `npm run build --workspace=backend`)
backend/dist/

# Runtime user uploads (files already tracked stay tracked)
backend/uploads/
